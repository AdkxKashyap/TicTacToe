const UserInfo = Object.freeze({
    FIRSTNAME: 'firstName',
    LASTNAME: 'lastName',
    USERNAME: 'userName',
    PASSWORD: 'password',
    HASHEDPW:'hashedPassword',
    TOKEN:'token',
    USERID:'userId',
});

export default UserInfo;